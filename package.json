{
  "name": "relia-bill",
  "private": true,
  "devDependencies": {
    "archiver": "^7.0.1",
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "dotenv-cli": "^10.0.0",
    "ignore": "^5.3.2"
  },
  "scripts": {
    "dev:api": "pnpm exec dotenv -e ./.env -- pnpm -C apps/api start:dev",
    "dev:web": "pnpm exec dotenv -e ./.env -- pnpm -C apps/web dev",
    "dev": "concurrently -n WEB,API -c blue,green \"pnpm run dev:web\" \"pnpm run dev:api\"",
    "build:web": "pnpm --dir apps/web build",
    "build:api": "pnpm --dir apps/api build",
    "build": "pnpm -r build",
    "db:migrate": "pnpm exec dotenv -e .env -- pnpm dlx prisma migrate dev --schema apps/api/prisma/schema.prisma",
    "db:studio": "pnpm exec dotenv -e .env -- pnpm dlx prisma studio --schema apps/api/prisma/schema.prisma",
    "db:generate": "pnpm exec dotenv -e .env -- pnpm dlx prisma generate --schema apps/api/prisma/schema.prisma",
    "login:token": "pnpm exec dotenv -e .env -- node scripts/dev-login.js",
    "api:me": "pnpm exec dotenv -e .env -- node scripts/api-me.js",
    "zip:project": "node ./scripts/zip-project.js",
    "send:acube": "node scripts/send-acube.js --pause"
  },
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.1"
  }
}
